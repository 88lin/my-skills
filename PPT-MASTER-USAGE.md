# ppt-master 使用说明

## 这是什么

`ppt-master` 不是普通的“小 skill”，它更像一套 **从原始资料到正式可编辑 PPTX 的完整生产系统**。

它的核心能力不是“简单做几页 PPT”，而是：

- 接收多种原始资料
- 把资料自动整理成适合演示文稿的结构
- 走一整套设计和生成流程
- 最后产出 **可在 Microsoft PowerPoint 里继续编辑的 `.pptx` 文件**

它的重点是：

- 不是图片塞进 PPT
- 不是网页 slides
- 不是只能套固定模板填空
- 而是尽量生成真正可编辑的 PowerPoint 内容

一句话理解：

**你手里有资料，最后要交一个正式、能继续编辑的 PPTX 成品，就优先考虑 `ppt-master`。**

---

## 它和你现有 skill 的区别

### `pptx`

- 更像通用 PowerPoint 文件处理 skill
- 适合改现有 `.pptx`
- 适合拆分、合并、整理、调整现成 PPT

### `html-ppt`

- 适合网页演示稿
- 适合浏览器直接打开的静态 HTML deck
- 不输出 `.pptx`

### `ppt-master`

- 适合从原始资料直接生成新的正式 PPTX
- 适合需要较完整流程的正式汇报成品
- 适合后续还要在 PowerPoint 里继续改的场景

一句话分流：

- 改现有 PPT：`pptx`
- 做网页演示稿：`html-ppt`
- 从资料直接生成正式可编辑 PPTX：`ppt-master`

---

## 什么时候优先用它

下面这些情况优先考虑 `ppt-master`：

- 你手里有 PDF、DOCX、Markdown、网页资料、公众号文章、纯文本笔记等原始内容
- 你最后要交付 `.pptx`
- 你希望演示文稿后续还能在 PowerPoint 里继续编辑
- 你要的是正式汇报、路演材料、课程讲稿、技术分享稿、发布会材料
- 你要的不是轻量草图，而是更接近成品级的演示材料

特别适合的场景：

- `把这份 PDF 白皮书做成投资人汇报 PPT`
- `根据这份 DOCX 方案做一个内部汇报稿`
- `把几个网页链接和一份提纲整理成正式演示文稿`
- `做完后老板还要在 PowerPoint 里继续改`

---

## 不适合什么时候用

下面这些情况不要优先用它：

- 你只是想改一个现有 `.pptx`
- 你只是想拆分或合并 PowerPoint 文件
- 你只想做浏览器演示稿
- 你只想做静态海报、封面、单页视觉稿
- 你只想快速做一个小网页演示，而不是交付 `.pptx`

这些情况下更适合：

- 改现有 PPT：`pptx`
- 做 HTML 演示稿：`html-ppt`
- 做静态视觉稿：`canvas-design`

---

## 支持哪些原始资料

根据仓库文档，`ppt-master` 能接受的来源很宽，常见包括：

- PDF
- DOCX
- PPTX
- EPUB
- HTML
- LaTeX
- RST
- URL（包括网页、文章链接等）
- Markdown
- 纯文本

你的理解可以简单一点：

**只要你手头有一堆内容素材，想把它们变成正式 PPTX，`ppt-master` 大概率都能接。**

---

## 它大致怎么工作

从仓库的 `SKILL.md` 看，它不是“随手生成几页”，而是有一条完整流水线：

`源资料 → 建项目 → 选模板/方向 → 策略规划 → 生成页面内容 → 后处理 → 导出 PPTX`

你不需要背内部每一步，但要知道：

- 它是流程型工具
- 适合完整任务
- 不适合“顺手改一下第 3 页”这种轻任务

所以用它时，最好把任务一次说清楚，不要零碎地下命令。

---

## 最推荐的提需求方式

为了让它跑得更稳，建议你描述清楚这 5 件事：

1. 原始资料是什么
2. 最终是不是要 `.pptx`
3. 使用场景是什么
4. 页数大概多少
5. 风格要求是什么

你可以直接这么说：

```text
用 ppt-master 把这份 PDF 和这份提纲做成正式可编辑 PPTX。
场景是内部项目汇报。
页数控制在 12 页左右。
风格要求：简洁、专业、不要太花。
```

或者：

```text
我有一份 DOCX 和几个网页资料，帮我用 ppt-master 做一套正式演示文稿。
最终要 PowerPoint 文件，后面还会继续在 Microsoft PowerPoint 里改。
```

或者：

```text
这次不要 HTML，我要最终能继续编辑的 PPTX，用 ppt-master。
```

---

## 使用技巧

### 1. 资料越完整，结果越稳

虽然它能处理很多来源，但你给的资料质量仍然很重要。

建议尽量给：

- 主体资料
- 一份结构提纲
- 目标页数
- 受众是谁
- 演示场景是什么

不要只丢一句：

```text
给我做个 PPT
```

这样它能做，但稳定性和方向感都会差很多。

### 2. 先说明“这是正式汇报”还是“这是分享稿”

同样一份资料，场景不同，输出会差很多。

比如：

- 内部管理汇报
- 技术分享
- 对外路演
- 课程讲稿
- 发布会材料

这些会影响：

- 结构
- 节奏
- 视觉风格
- 信息密度

### 3. 明确是否要后续继续编辑

如果你不强调这一点，系统可能默认你只是要成品效果。

建议直接说：

```text
后面还要继续在 PowerPoint 里改
```

这句话会帮助它更明确地朝“正式可编辑 PPTX 成品”方向去做。

### 4. 它更适合“成套做”，不适合“零碎修”

下面这种适合它：

- 从一堆资料生成一整套 PPT
- 做正式汇报成品

下面这种不适合它：

- 把第 4 页标题改一下
- 统一一下现有 PPT 的页脚
- 拆一下两个现成 PPT 文件

这些更适合 `pptx`。

### 5. 可以把它理解成“PPT 工程系统”

不要把它当成一个“比普通 skill 稍重一点”的小工具。

更好的理解是：

- 它像一个专门为正式 PPT 生产准备的工具链
- skill 只是入口
- 真正工作依赖整仓库结构和 Python 环境

---

## 它的优势

- 最终产物是原生可编辑 PPTX
- 更适合正式交付
- 可以从多种资料格式出发
- 不是简单模板填空，也不是图片拼页
- 更像完整的 PPT 生产流水线
- 仓库里还有模板、动画、转场、格式、脚本等延展能力
- 支持语音旁白和视频导出方向的扩展能力

---

## 它的代价

- 依赖更重
- 工作流更长
- 不适合所有轻量演示需求
- 学习成本比普通 skill 高
- 管理方式不能像普通 skill 那样只更一个 `SKILL.md`

所以它不是默认主力 PPT skill，而是：

**正式可编辑 PPTX 成品场景下的重型专用方案。**

---

## 在你这套系统里的定位

- 管理方式：外部独立仓库管理
- 不纳入普通 `skills-cli` 自动更新链路
- 本地仓库：`C:\Users\Computer\.agents\external\ppt-master`
- skill 入口：`C:\Users\Computer\.agents\external\ppt-master\skills\ppt-master\SKILL.md`
- 独立虚拟环境：`C:\Users\Computer\.agents\external\ppt-master\.venv`

一句话：

**它已经安装好了，但它不是你那套普通“单 skill 自动更新”的成员。**

---

## 为什么单独管理

原因有 3 个：

1. 它依赖整仓库结构，不只是一个 `SKILL.md`
2. 它有自己的 Python 依赖
3. 它更像一个完整工具系统，不适合用普通单 skill 更新方式维护

补充一点：

之前安装它时，我已经给它建了独立虚拟环境，这样可以避免它的 Python 依赖继续污染你机器上的其他工具环境。

---

## 更新方式

### 更新仓库

```powershell
git -C "C:\Users\Computer\.agents\external\ppt-master" pull --ff-only origin main
```

### 更新依赖

```powershell
& "C:\Users\Computer\.agents\external\ppt-master\.venv\Scripts\python.exe" -m pip install -r "C:\Users\Computer\.agents\external\ppt-master\skills\ppt-master\requirements.txt"
```

建议你把这两步当成一组操作：

- 先 `git pull`
- 再补一次 `pip install -r ...`

这样最稳。

---

## 当前本地使用建议

如果以后你要用它，我建议优先这样说：

```text
用 ppt-master 把这些资料做成正式可编辑 PPTX。
```

然后补上：

- 资料来源
- 场景
- 页数
- 风格
- 是否继续编辑

### 推荐模板化提问

```text
用 ppt-master 做一套正式可编辑 PPTX。
资料来源：PDF + DOCX + 网页链接
场景：内部项目汇报
页数：10~15 页
风格：简洁、专业、信息密度中等
要求：后续还要继续在 PowerPoint 里编辑
```

---

## 不要这样用

下面这些场景不要优先动用它：

- `帮我把第 3 页字改一下`
- `统一一下这个现有 PPT 的配色`
- `拆成两个 PowerPoint 文件`
- `做个浏览器里讲的分享页`
- `做个 PPT 风格的单页网站`

这些分别更适合：

- 改现有 PPT：`pptx`
- 网页演示稿：`html-ppt`
- PPT 风格静态 HTML 页面：`html-ppt`

---

## 最后总结

把 `ppt-master` 记成这句话就够了：

**当你手里已经有资料，最后要交一个正式、可编辑、还能继续在 PowerPoint 里改的 PPTX 成品时，用它。**
